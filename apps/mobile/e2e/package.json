{
  "name": "@spectra/mobile-e2e",
  "private": true,
  "scripts": {
    "e2e:android:build": "detox build -c android.emu.debug",
    "e2e:android:test": "detox test -c android.emu.debug -w 1",
    "e2e:ios:build": "detox build -c ios.sim.debug",
    "e2e:ios:test": "detox test -c ios.sim.debug -w 1"
  },
  "devDependencies": {
    "detox": "^20.13.0",
    "jest": "^29.7.0"
  }
}
